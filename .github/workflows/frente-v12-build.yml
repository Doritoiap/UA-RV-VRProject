name: Build Frente v1.2 APK

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Assemble v1.2 source
        run: |
          cat frente-v2-java/part* > frente-build/app/src/main/java/com/nacho/frente/MainActivity.java
          python frente-ui-v12.py
          sed -i "s/versionCode 1/versionCode 3/; s/versionName '1.0'/versionName '1.2'/" frente-build/app/build.gradle
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - uses: android-actions/setup-android@v3
      - run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Build APK
        working-directory: frente-build
        run: gradle --no-daemon assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Frente-v1.2-apk
          path: frente-build/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
